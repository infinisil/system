#!/usr/bin/env nix-shell
#!nix-shell -i bash -p nixops


if [ "$1" = "b" ]; then
	build="--build-only"
	printf "Only building "
	shift
else
	printf "Building and activating\n"
fi


NIXOPS_DEPLOYMENT=${NIXOPS_DEPLOYMENT:-infinisil} \
sudo nixops deploy $build $@
